{
  "name": "trueleqtric-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd server && npx prisma generate",
    "install-server": "cd server && npm install"
  }
}
